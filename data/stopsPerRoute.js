// Learns which stops are supported by which routes, and saves them in a local
// JSON file.

// var routes = require('../crawler/config.json').routes;
//
// // Prepare data logs / db for access.
// const INTERFACE = require("../data-interface/data-interface-main.js");
// const stoplog = new INTERFACE('stoplog');
//
// var routeInfo = require('./routeInfo.json');
//
// if (!routeInfo.route) routeInfo.route = {};
// if (!routeInfo.stopID) routeInfo.stopID = {};
//
// // Initial JSON set up.
// for (var i = 0; i < routes.length; i++){
//
//   // Check if there is a valid route entry in JSON file, if not add it.
//   if (!routeInfo.route[routes[i]]) routeInfo.route[routes[i]] = [];
//
//   // Get all of the stops made at a certain route.
//
//
// }
